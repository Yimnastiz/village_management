"use client";
import { useEffect, useRef, useState } from "react";
import { createPortal } from "react-dom";
import { Plus } from "lucide-react";
import { Button } from "@/components/ui/button";
import { CreateAppointmentForm } from "./create-appointment-form";
export function CreateAppointmentButton() { const [open, setOpen] = useState(false); const triggerRef = useRef<HTMLButtonElement>(null); const close = () => { setOpen(false); requestAnimationFrame(() => triggerRef.current?.focus()); }; useEffect(() => { if (!open) return; const keydown = (event: KeyboardEvent) => { if (event.key === "Escape") close(); }; document.addEventListener("keydown", keydown); return () => document.removeEventListener("keydown", keydown); }, [open]); const modal = open && typeof document !== "undefined" ? createPortal(<div className="fixed inset-0 z-[200] flex items-end justify-center bg-black/45 p-0 sm:items-center sm:p-4" role="dialog" aria-modal="true" aria-labelledby="create-appointment-title"><button type="button" aria-label="ปิด" className="absolute inset-0 cursor-default" onClick={close} /><div className="relative z-10 flex max-h-[92dvh] w-full max-w-xl flex-col rounded-t-2xl bg-white shadow-2xl sm:rounded-2xl"><div className="flex items-center justify-between border-b border-gray-200 px-5 py-4"><h2 id="create-appointment-title" className="text-lg font-semibold">สร้างนัดหมายให้ลูกบ้าน</h2><button type="button" aria-label="ปิด" onClick={close} className="rounded-lg px-2 py-1 text-sm text-gray-500 hover:bg-gray-100">ปิด</button></div><div className="overflow-y-auto p-5"><CreateAppointmentForm onClose={close} /></div></div></div>, document.body) : null; return <><Button ref={triggerRef} size="sm" onClick={() => setOpen(true)}><Plus className="h-4 w-4" /> สร้างนัดหมาย</Button>{modal}</>; }
