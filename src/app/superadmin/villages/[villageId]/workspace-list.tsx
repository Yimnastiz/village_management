import Link from "next/link";
import { Badge } from "@/components/ui/badge";

export type WorkspaceListItem = { id: string; title: string; description?: string | null; status?: string | null; meta?: string | null; href?: string };
export function WorkspaceListPage({ title, description, basePath, query, status, statuses, items, emptyText }: { title: string; description: string; basePath: string; query: string; status: string; statuses?: { value: string; label: string }[]; items: WorkspaceListItem[]; emptyText: string }) {
  return <div className="space-y-4"><div><h2 className="text-xl font-semibold text-slate-950">{title}</h2><p className="mt-1 text-sm text-slate-500">{description}</p></div><form action={basePath} className="grid gap-2 rounded-xl border bg-white p-3 sm:grid-cols-[1fr_auto_auto]"><input type="search" name="q" defaultValue={query} placeholder="ค้นหารายการ" className="min-h-10 min-w-0 rounded-lg border border-slate-300 px-3 text-sm" />{statuses ? <select name="status" defaultValue={status} className="min-h-10 rounded-lg border border-slate-300 bg-white px-3 text-sm"><option value="ALL">ทุกสถานะ</option>{statuses.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select> : <span />}<div className="flex gap-2"><button className="rounded-lg bg-slate-900 px-4 text-sm font-medium text-white">ค้นหา</button><Link href={basePath} className="inline-flex items-center rounded-lg border px-3 text-sm">ล้าง</Link></div></form>{items.length ? <div className="overflow-hidden rounded-xl border border-slate-200 bg-white"><div className="divide-y divide-slate-100">{items.map((item) => { const body = <div className="flex flex-col gap-2 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="min-w-0"><p className="truncate font-medium text-slate-900">{item.title}</p>{item.description ? <p className="mt-1 line-clamp-2 text-sm text-slate-500">{item.description}</p> : null}{item.meta ? <p className="mt-1 text-xs text-slate-400">{item.meta}</p> : null}</div>{item.status ? <Badge variant="outline">{item.status}</Badge> : null}</div>; return item.href ? <Link key={item.id} href={item.href} className="block hover:bg-slate-50">{body}</Link> : <article key={item.id}>{body}</article>; })}</div></div> : <div className="rounded-xl border border-dashed bg-white p-10 text-center text-sm text-slate-500">{emptyText}</div>}</div>;
}
