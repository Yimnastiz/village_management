import {AuditAction} from "@prisma/client";import {NextResponse} from "next/server";import {buildVillagePopulationWorkbook,parsePopulationExportOptions} from "@/features/population/server/population-export";import {prisma} from "@/lib/prisma";import {requireSuperAdminRequestSession} from "@/lib/superadmin";
export async function GET(request:Request,{params}:{params:Promise<{villageId:string}>}){const actor=await requireSuperAdminRequestSession(request);if(!actor)return NextResponse.json({error:"Unauthorized"},{status:401});const {villageId}=await params;const village=await prisma.village.findUnique({where:{id:villageId},select:{id:true}});if(!village)return NextResponse.json({error:"Village not found"},{status:404});const options=parsePopulationExportOptions(new URL(request.url),false);const result=await buildVillagePopulationWorkbook(villageId,options);await prisma.auditLog.create({data:{userId:actor.id,villageId,action:AuditAction.POPULATION_EXPORT_CREATED,resource:"PopulationExport",metadata:{actorRole:"SUPERADMIN",targetVillageId:villageId,...options,counts:result.counts}}});return new NextResponse(result.buffer,{headers:{"Content-Type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","Content-Disposition":`attachment; filename="population-export-${new Date().toISOString().slice(0,10)}.xlsx"`,"Cache-Control":"no-store"}})}
