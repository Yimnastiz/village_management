"use client";import {useState} from "react";import {Button} from "@/components/ui/button";import {useToast} from "@/components/ui/toast";
export function ExportDownload({href}:{href:string}){const [pending,setPending]=useState(false);const toast=useToast();return <Button isLoading={pending} onClick={async()=>{setPending(true);try{const response=await fetch(href);if(!response.ok)throw new Error();const blob=await response.blob();const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=`population-export-${new Date().toISOString().slice(0,10)}.xlsx`;a.click();URL.revokeObjectURL(url);toast.success("เตรียมข้อมูลส่งออกสำเร็จ")}catch{toast.error("ส่งออกไม่สำเร็จ","กรุณาลองใหม่อีกครั้ง")}finally{setPending(false)}}}>ดาวน์โหลด Excel</Button>}
